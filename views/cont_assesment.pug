extends layout

block content
    .header 
        a(href="/logout" class="text-center fixed-top bg-secondary text-light fw-bolder p-2 text-decoration-none fs-5") Log Out
    .welcome-box(class="container")
        h2(class="text-center mt-4") #{message}📝